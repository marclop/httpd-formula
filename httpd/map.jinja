{% set apache = salt['grains.filter_by']({
    'Debian': {
        'server': 'apache2',
        'service': 'apache2',
         'conf': '/etc/apache2/apache.conf',
    },
    'RedHat': {
        'server': 'httpd',
        'service': 'httpd',
        'conf': '/etc/httpd/conf/httpd.conf',
        'conf_dir': '/etc/httpd/conf.d'
    },
}, merge=salt['pillar.get']('apache:lookup'), default='RedHat') %}
